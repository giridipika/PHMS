<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.phms">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PHMS">
        <activity android:name=".NotesEditor"
            android:label="@string/notesText"/>
        <activity android:name=".DietActivity"
            android:label="@string/dietText"/>
        <activity android:name=".ExerciseActivity"
            android:label="@string/exerciseText"/>
        <activity android:name=".NotesActivity"
            android:label="@string/notesText"/>
        <activity android:name=".MedicationActivity"
            android:label="@string/medicationText"/>
        <activity android:name=".VitalActivity"
            android:label="@string/vitalsText"/>
        <activity
            android:name=".ui.login.SignUpActivity"
            android:label="@string/title_activity_sign_up" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>